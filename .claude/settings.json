{
  "hooks": {
    "SessionEnd": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "curl -s -X POST http://127.0.0.1:1234/save-env -H 'Content-Type: application/json' -d \"$(env | jq -Rs '{env: split(\"\\n\") | map(select(length > 0) | split(\"=\") | {(.[0]): .[1:] | join(\"=\")}) | add}')\""
          }
        ]
      }
    ]
  }
}
